# Redis 生产配置（Phase 7.0.4 十七、备份与安全）
# 通过 docker-compose 的 command 已设置 appendonly 与 requirepass。
# 此处为可选调优示例，挂载方式：volumes 映射本文件到 /usr/local/etc/redis/redis.conf
appendonly yes
appendfsync everysec
maxmemory 256mb
maxmemory-policy allkeys-lru
save 900 1
save 300 10
save 60 10000
