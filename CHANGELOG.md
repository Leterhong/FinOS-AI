# Changelog

本项目的所有重要变更都记录在此文件中。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## [2.2.0] - 2026-09-01

### 企业治理与质量控制

- 新增企业治理工作台：组织成员、Owner/Admin/Analyst/Reviewer/Viewer 五级角色、项目最小授权、公开/内部/机密/严格受限四级数据分级和统一治理审计。
- 成员可在已授权组织间切换治理视图；历史项目与规则首次进入时安全归并到所属默认组织，避免升级后无法授权。
- 新增规则完整快照历史与历史版本回放；关键资料、风险、报告、流程和模型评测统一进入需要实名意见的人工复核队列。
- 新增可复现模型评测集、真实默认模型执行记录、关键词评分、提示注入/凭据外传/工具越权防护和评测复核留痕。
- 新增受控 JSON/CSV 企业数据源连接器：凭据加密、公网 SSRF 校验、禁止重定向、5MB/500 条上限、同步资料继承项目密级并强制复核。
- 新增治理可观测性：请求端点、AI 调用量/Token/平均耗时、待复核数、连接器失败数和审计事件汇总。

### OCR 与证据结构

- 资料研判支持 PNG/JPEG/WebP 视觉 OCR，记录提取方式和 OCR 状态；结构化事实落地页码、行号、表格单元格或归一化图像坐标。
- Excel/CSV/模型抽取结果保留表头、行列与页/工作表信息，并在资料页面提供结构化表格核验视图。
- 新增 `7f17governance` 数据迁移、治理后端回归测试和提示词防护单元测试。
- 修复历史 `7f04` 迁移对启动期建表顺序的隐式依赖及 `7f13` 默认值拼写错误；Alembic 现在可以从真正的空数据库升级到最新版本，并加载完整模型元数据。

### 企业研判闭环

- 新增项目级研判工作台：集中展示资料、结构化事实、财务指标、跨期趋势、候选风险、规则、任务、Agent 运行、研究底稿与审计摘要，并支持 Markdown 报告导出。
- 资料事实持久化原文、位置、单位、报告期、置信度和人工复核记录；AI 输出登记为候选风险，确认与缓释必须填写操作人和意见。
- 规则由真实测试样本驱动覆盖率，保存版本、测试人、期望/实际结果；人工任务支持说明、改派、延期、退回、阶段调整和操作历史。
- 批量资料分析增加逐项状态与失败重试；模型中心增加任务角色路由、最近错误和健康信息。

### 财务与交付

- 新增流动比率、资产负债率、净利率、应收账款周转率和经营现金流覆盖等确定性指标；缺科目、分母为零或事实被驳回时不计算。
- 项目报告保留事实 ID、规则版本、Agent 运行 ID、风险复核和免责声明。

### 工程与合规

- 新增 `7f16evidence` 迁移与开发库自愈补列，服务端完整保存证据、风险复核、规则测试和任务历史。
- 新增单机体验/企业生产模式说明与应用内上线控制清单，重写隐私说明和服务条款。
- 移除最后一批未引用的个人财富前端源码；旧数据库表仅为迁移兼容保留。
- 新增财务分析与报告单测；完整端到端主链路保持 12/12 通过。

## [2.1.0] - 2026-08-30

在 2.0.1 修复基础上补齐工程质量与产品深度：修复 CI 根因（gitignore 误伤源码目录）、为审查修复建立回归测试网，并落地「服务端持久化 + 结构化抽取 + 确定性规则引擎 + SSE 流式」四项核心能力。

### 修复

- **CI 根因**：`.gitignore` 的 `storage/`、`backup/` 模式未锚定根路径，把 `src/financial-data/storage/` 与 `backend/backup/` 两个源码目录静默排除在仓库之外——任何 fresh clone 都无法编译，CI 首跑暴露了这一自 2.0.0 起就存在的问题。已改为仅匹配根目录运行时数据目录。
- 企业 AI 路由捕获模型配置解密失败并返回 503（此前为裸 500）。
- 调度守护线程增加原子认领（多 worker 下同一到期任务不再双跑）；限流切换为 Redis 滑动窗口（跨进程共享配额，fail-open 降级内存）。
- metrics 路径归一化覆盖前缀 ID 与带连字符 UUID，统计容量超限开始计数并告警。
- `npm test` 显式指定测试文件（Node 20 + Windows 可直接运行）；`@eslint/eslintrc` 声明进 devDependencies。

### 新增

- **服务端持久化**：`backend/enterprise` 模块（项目/资料/风险/规则/任务/投研底稿 6 张表 + upsert/删除/快照 API + Alembic 迁移），全部绑定 user_id、统一 404 防枚举；前端 `enterprise-sync` 层在本地乐观更新之上后台推送，启动时拉取快照实现跨设备恢复，后端不可达时静默降级。
- **确定性规则引擎**：规则支持结构化触发条件（指标 + 比较算子 + 阈值），命中判定由 `src/lib/rule-engine.ts` 纯函数完成（万/亿单位归一化、无引用不算命中、三种结论显式）——规则匹配从 prompt 祈祷变成可测试代码。
- **结构化抽取**：资料研判改为两段式——先以 JSON 约束抽取事实（逐条原文引用），再确定性评估规则，最后生成叙述；「已抽取事实」指标恢复为真实数据。
- **SSE 流式**：`/api/enterprise/ai` 支持 `stream:true` 逐段转发；智能助手逐字渲染，流式中断时保留已生成部分。
- **端到端测试**：`tests/e2e/main-chain.mjs` 用本地 mock LLM 拉起真实前后端进程，覆盖会话→模型→对话→流式→资料研判→后端持久化→用户隔离 12 项断言，接入 CI。

### 工程

- 新增回归测试 13 条（AI 网关同步封装/风险评分/目标解析/金额解析/调度认领/metrics/配置守卫）；契约测试新增 nginx 孤岛路由与 compose 令牌有效期守卫；规则引擎单测 4 条（tsx）。
- 新增 GitHub Actions CodeQL 静态扫描与 Dependabot 依赖更新；引入 ruff 基线检查；requirements.txt 收敛上界。
- 6 篇 1.x 时期文档加历史归档标记。
- CI Python 固定为 3.12（与本地验证环境一致）。

---

## [2.0.1] - 2026-08-29

全代码库审查后的修复与加固版本：修复 Docker 部署下企业 AI 主链路 404、三处 AI 网关调用缺陷与多处金融计算错误，补齐 SSRF 出站边界、账户删除与弱密钥守卫，并移除约 1.2 万行不可达的遗留个人理财代码。

### 修复（关键）

- Docker：nginx 孤岛路由补上 `/api/enterprise` 与 `/api/workspace`，修复一键部署后智能助手、资料研判、Agent、投研与模型中心全部 404 的问题。
- 后端：CFO 分析、Agent 编排总结、RAG 回答三处对异步网关未 `await` 即取值，配置模型后必然 500；统一改为同步网关封装。
- 后端：异步文档解析任务把 `user_id` 字符串当 `User` 对象传入，任务 100% 失败。
- 后端：自动化动作 `create_task` 参数错位，产生的任务类型非法且对所有用户可见。
- 前端：模型配置解密失败时不再静默清空用户 API Key（现返回 503 并拒绝写入）。

### 安全

- 修复自动化规则/工作流出站 Webhook 绕过 SSRF 校验的问题（统一走公网地址校验 + 禁止重定向）。
- 模型 Base URL 增加服务端出站校验：拒绝非 HTTP(S)、云元数据与链路本地地址；本机/内网地址默认仅开发环境放行（`AI_ALLOW_PRIVATE_ENDPOINTS` / `FINOS_ALLOW_PRIVATE_AI_ENDPOINTS` 可显式放行）；不再回显上游响应体。
- 弱密钥守卫收录仓库自带占位值并按特征识别，前端四个加密模块同步守卫。
- 账户删除覆盖全部 41 张用户数据表、多模态加密文件与文档原件，并清除 Refresh Token。
- Refresh Token 轮换改为条件原子更新，消除并发重放双花竞态。
- 限流、审计与 metrics 统一可信代理 IP 判定；CSRF 比较改为常量时间；限流器与 metrics 统计表限制内存增长；`/api/metrics` 仅限运维 Key 访问；`/api/health` 不再回传内部异常详情。
- 启动期敏感数据迁移失败改为显式记录；Alembic 迁移失败不再静默降级 `create_all`。
- `.dockerignore` 排除子目录环境文件，防止 `backend/.env` 进入镜像层。

### 金融逻辑

- 财富数字分身：负债风险分量符号修正（此前恒为 0）；目标完成度统一使用可解析「亿/万/年龄/年限」的解析器；快照包含风险分与目标进度，监控对比不再每次误报，重复告警 24 小时去重，快照历史自动裁剪。
- 多模态确认：单笔流水不再覆盖月度收支画像；「年终奖 50000 元」不再被改写成 4166 存库，仅「月薪/月收入」等聚合口径才更新画像。
- 资料解析：多编码解码（UTF-8/GB18030）、BOM 处理、「亿」单位、日期列不再被当作金额、xlsx 用 openpyxl、PDF 用 pypdf、docx 用 python-docx；确认金额做数值与符号校验。
- 投研建议：「10 年后净资产」不再取 30 年期里程碑；自动化工作流事件条件兼容 camelCase 事件字段。
- 其他：内存缓存支持 TTL；SSE 流式按行拆分避免多行内容丢失；调度守护线程随应用启动（定时自动化此前从未运行）；定时任务按服务器本地时钟解释；每日简报兼容新旧严重度词表并按本地时区生成；Agent 缓存键改用 sha256。

### 产品体验

- 项目进度由资料解析、风险核验、任务推进三类真实事实推导（此前永远 0%）；规则库支持「标记为已测试 / 删除」的生命周期；流程任务超期按真实日期判定并支持选择关联项目。
- 智能助手对话历史持久化，修复中文输入法回车误发送；顶栏 ⌘K 真正聚焦搜索，搜索范围覆盖项目、资料与风险；Agent 研判输出可一键转为风险信号或流程任务；风险支持「标记已缓释」；对话框支持 Esc 关闭。
- 清理约 1.2 万行不可达的遗留个人理财页面、组件、store 与 API 路由，前端源文件从 339 个降至 95 个。

### 工程

- 新增 GitHub Actions CI（typecheck / npm test / build / pytest）；`npm test` 改为目录形式，Node 20 与 Windows 可直接运行。
- 修复备份/恢复脚本的服务名错误；监控抓取补充认证说明并修正目标；compose 透传 `AI_MAX_TOKENS` / `AI_MAX_INPUT_CHARS` / `TRUSTED_PROXY_IPS`，Access Token 默认有效期改为 15 分钟；Next.js 关闭 `X-Powered-By` 并输出基础安全响应头；`npm ci` 失败不再静默降级。

---

## [2.0.0] - 2026-08-26

FinOS AI 从个人财富工具升级为面向企业经营与风险研判的金融服务 Agent。

### 新增

- 企业经营决策台、项目中心、资料研判、风险中心、投研中心、规则库、Agent 中心、流程中心和智能研判助手。
- 企业工作区本地持久化：项目创建、资料上传、风险核验、规则新增、任务流转、Agent 运行与研究底稿均可交互并在刷新后保留。
- 无登录开源体验和新的 FinOS AI 企业品牌标识。

### 修复

- 修复 `502 → 409 → 多接口 401` 会话错误级联，以及 Windows `localhost` IPv6 解析问题。
- Refresh Token 改为 HttpOnly Cookie，Access Token 仅驻留内存。
- 加固文件路径、上传大小、内网 URL、可信代理和异步任务认领。
- 修复 Docker 同源 API 地址重复 `/api/api` 的部署配置。

### 变更

- 移除登录与注册页面，旧个人财富路由统一重定向到企业模块。
- README、产品定位和运行说明全面切换为企业金融 Agent。

---

## [1.0.0] - 2026-07-30

FinOS AI 首个正式版本。一个**完全开源、自托管、无付费墙**的个人财富操作系统：
把分散的资产、现金流与目标汇聚成可对话、可推演、可主动提醒的财富数字分身。

### 核心能力

- **财富数字分身（Financial Twin）** —— 5 步向导建模资产 / 负债 / 收支 / 目标，
  统一净值口径与结构分解。零数据时展示空态引导，不填充任何默认财富数字。
- **六维财富评分与三段式推理** —— 现金流、抗风险、投资结构、负债、储蓄、目标达成六个维度，
  每个结论都给出「现状 → 归因 → 建议」，可追溯到具体数据项。
- **情景模拟与目标推演** —— 调节收入、支出、收益率、通胀，实时计算目标达成概率与敏感度。
- **AI 助手** —— 基于用户真实数据的对话；多模态识别录入（账单 / 截图 / 文本），
  识别结果**必须用户确认后**才写入分身。
- **智能体生态** —— 现金流、投资、保险、退休、税务五个专项 AI CFO，
  支持单体运行与多体协同工作流。
- **智能自动化与主动服务** —— 事件规则、定时任务、多体工作流、条件 DSL、
  执行计划与行动中心；用户的「采纳 / 忽略」反馈回流成偏好画像。
- **个人 OS** —— 财富时间线、长期记忆、私有知识库（RAG，回答标注来源）、
  每日简报、指挥中心与全局搜索（`⌘K`）。
- **模型中心（BYOM）** —— 应用内填写自己的 API Key，支持任何 OpenAI 兼容协议服务
  （官方 API、国内厂商、本地 Ollama / vLLM）。项目不内置、不代理、不转售任何模型服务。
- **报告生成** —— 多种类型的财富报告，可持久化与导出。

### 技术架构

- 前端：Next.js 15 App Router + React 19 + TypeScript strict + Tailwind CSS +
  Zustand + React Query v5 + Framer Motion。25 个受保护路由，60 个 Next 路由处理器。
- 后端：FastAPI + SQLAlchemy 2.0，模块化单体，13 个业务路由模块、44 张数据表。
  开发用 SQLite，生产用 PostgreSQL。
- 部署：Docker Compose 五服务编排（frontend / backend / postgres / redis / nginx），
  `deploy.sh` 一键部署并自动生成全部随机密钥。
- 测试：112 项自动化测试（后端 API + AI 质量层 + 前端契约层），
  全部可在**不连接任何大模型、不依赖外部服务**的环境下运行。

### 安全与隐私

- **字段级加密** —— 资产金额、收入、模型 API Key 等敏感字段以 AES-256-GCM 透明加密落盘。
- **用户隔离铁律** —— 所有业务查询强制绑定 `user_id`；越权访问统一返回 404，
  避免形成资源存在性侧信道。
- **密钥零泄露** —— 模型 API Key 仅以 `sk-****last4` 掩码回显；
  任何接口与数据导出都不含明文，并有专项测试守卫。
- **JWT 双令牌** —— Access / Refresh 分离，Refresh Token 轮换与吊销（仅存 jti）。
- **纵深防护** —— CSRF 双提交校验、滑动窗口限流、上传防护、结构化日志脱敏。
- **生产启动守卫** —— 弱密钥或未配置密钥时在生产环境**直接拒绝启动**，
  而非静默降级为可伪造的默认值。
- **可导出可删除** —— 完整数据导出与账户注销（需输入确认短语）。

### 产品底线

- 无套餐、无订阅、无会员、无交易撮合，MIT 协议功能全开。
- 所有输出均标注「FinOS AI 提供信息分析和辅助决策，不构成投资建议」。
- 禁用「绝对安全 / 百分百 / 保证收益」等违规表述，有契约测试强制守卫。
- 系统永不代用户执行任何交易。

---

### 发布前审计修复（Phase 7.9）

v1.0.0 发布前完成了一轮全量产品审计，覆盖功能链路、按钮真实性、模拟数据、
API 完整性、数据库、AI 系统、异常处理、性能、部署、安全与文档共 14 个方面。
本轮修复项：

#### 安全

- **JWT 密钥启动守卫** —— `jwt_secret` 默认值由硬编码的 `CHANGE_ME_IN_ENV` 改为空值，
  并加入 fail-fast 校验：弱密钥或长度不足 32 字节时，生产环境拒绝启动，
  开发环境生成进程内临时密钥并告警。此前生产漏配会静默使用人人可知的密钥签发令牌。
- **运行指标端点访问控制** —— `/api/metrics` 此前无鉴权暴露运行时指标，
  现要求 `X-Metrics-Key` 或有效 Bearer Token，未授权返回 403。

#### 稳定性

- **智能体临时记忆内存泄漏** —— `backend/services/agent/memory.py` 的会话字典无 TTL、
  无上限、无淘汰，长时间运行必然泄漏。改为 `OrderedDict` + 锁 + LRU/TTL 淘汰
  （上限 256 会话，30 分钟过期）。

#### 真实性

- **旧版智能体占位实现下线** —— `backend/services/agent/executor.py` 中
  `monitor / risk / investment / retirement / cashflow / advisor` 此前返回固定拼接字符串，
  却仍通过 `/api/agent/tasks` 对外服务。现全部改为调用真实智能体生态，
  返回基于用户数据的评分与三段式结论。
- **模拟数据前端标识** —— 此前 `simulated` 标记只存在于数据层与注入 LLM 的文本中，
  用户可见界面**没有任何提示**。新增 `SimulatedDataNotice` 组件，
  在 AI 助手对话、智能体工具追踪、报告消息的工具调用卡片处显式标注；
  同时修正「工具调用（实时数据）」这一误导性标题，并让 `/api/financial-data/quotes`
  透传 `simulated` 字段。
- **报告页脚文案** —— 「仅供演示参考」改为标准合规免责声明。

#### 体验

- **全局 404 页面** —— 此前缺失 `not-found.tsx`，访问无效路由落到 Next 默认页造成视觉断层。
- **弹窗提示统一** —— AI 助手页 4 处原生 `alert()` 改为页面内 Toast，
  与全站提示风格一致。

#### 文档

- 新增本 CHANGELOG。
- README 补充完整「使用指南」章节（从注册到日常使用的四步路径），
  并新增数据源与模拟数据的显式披露，说明 `FINOS_MARKET_PROVIDER` /
  `FINOS_STOCK_PROVIDER` / `FINOS_FUND_PROVIDER` 三个切换开关。
- 修正 README 安全章节中「未连接数据源时绝不生成模拟行情」这一与实现不符的表述，
  改为准确说明：用户自身财务数据绝不虚构，外部市场数据在未接入真实源时由模拟
  Provider 生成且全程带标识。

---

## 版本说明

`1.0.0` 之前的所有开发阶段（Phase 1 至 Phase 7.8）在本仓库中作为单一初始提交发布，
不单独列出历史版本。后续版本将在此文件中按语义化版本逐条记录。

[1.0.0]: https://github.com/Leterhong/FinOS-AI/releases/tag/v1.0.0
