# Security Policy / 安全策略

FinOS AI 处理的潜在数据包括企业经营资料、财务信息、风险规则、研究底稿和模型凭据。我们重视负责任的漏洞披露，并希望安全研究不会让真实企业、个人或第三方系统承担风险。

## 支持范围

| 版本 | 安全更新 |
| --- | --- |
| `main` / 最新 `2.x` | 支持 |
| `1.x` 及更早版本 | 不支持 |

请先在最新代码上确认问题。仅存在于旧版本、已修改分支或第三方部署配置中的问题，可能不在项目修复范围内。

## 私密报告漏洞

请使用 GitHub 的 [Private vulnerability reporting](https://github.com/Leterhong/FinOS-AI/security/advisories/new) 提交安全报告。不要为未修复漏洞创建公开 Issue、Discussion 或 Pull Request。

报告中请尽量包含：

- 受影响的版本、提交哈希和组件；
- 部署方式与必要的非敏感配置；
- 可重复的最小步骤或概念验证；
- 预期行为、实际行为和潜在影响；
- 建议修复方式（如有）；
- 已经采取的任何临时缓解措施。

请对截图、请求、日志和样本文件进行脱敏。不要提交真实企业文档、个人信息、访问令牌、模型密钥、数据库凭据或其他第三方秘密。

## 处理流程

维护者会尽力：

1. 在 3 个工作日内确认收到报告；
2. 在 7 个工作日内完成初步分级并说明后续安排；
3. 根据影响、利用难度和修复风险协调补丁与披露时间；
4. 在修复可用后发布说明，并在报告者同意时致谢。

以上是社区项目的处理目标，不构成服务等级承诺。复杂问题或跨依赖漏洞可能需要更长时间。

## 优先关注的安全问题

- 身份认证、会话轮换、CSRF 或权限绕过；
- 跨组织、跨用户或跨项目的数据访问；
- 企业资料、日志、模型密钥或数据库凭据泄漏；
- 文件上传、目录穿越、任意文件读取或解析器利用；
- SSRF、开放重定向或对内网服务的非预期访问；
- Agent 工具越权、未经确认的高影响操作或审计绕过；
- 提示词注入导致的数据外传、权限提升或非预期工具调用；
- 可远程利用的依赖、容器、代理或默认配置问题。

单纯的模型幻觉、回答质量、演示数据不准确，若不造成权限、机密性、完整性或可用性影响，通常不属于安全漏洞。

## 安全研究准则

- 只测试你拥有或明确获准测试的环境与数据；
- 使用虚构、合成或已经脱敏的数据；
- 不进行拒绝服务、破坏性测试、社工、钓鱼或供应链攻击；
- 不访问证明漏洞所需范围之外的数据；
- 一旦发现真实数据、凭据或跨租户访问，立即停止并私密报告；
- 给维护者合理的修复时间后再讨论公开披露。

遵守上述准则并善意开展的研究，我们会视为负责任的安全研究。本项目目前不设漏洞奖金计划，也不授权测试任何第三方基础设施或公开演示实例。

## 已知产品边界

FinOS AI 2.0 的无登录工作区是用于开源体验和方案验证的演示模式，数据保存在浏览器本地。它不是已经完成企业多租户、RBAC、数据分级和审批隔离的 SaaS 服务，不应在公共或共享设备上录入真实敏感资料。

自托管部署者负责 HTTPS、网络边界、强密钥、数据库与备份权限、组织访问控制、日志保留、第三方模型数据政策和适用法规。详细控制与上线检查清单见 [`docs/security.md`](./docs/security.md)。
