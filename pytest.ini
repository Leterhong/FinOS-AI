[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q --tb=short --strict-markers
markers =
    slow: 耗时较长的用例
    integration: 需要真实外部依赖（默认跳过）
filterwarnings =
    ignore::DeprecationWarning
